# Screenshots

Drop the real app captures here with these exact filenames (they are referenced from the root `README.md`):

- `today-screen.png` — Today tab with habits checked off and streak counters
- `groups-leaderboard.png` — a group's leaderboard
- `activity-feed.png` — real-time group activity feed
- `dark-mode.png` — any screen in dark mode

Phone portrait captures (roughly 9:19.5) look best. The demo GIF referenced from the root README lives one level up, at `docs/demo.gif`.
